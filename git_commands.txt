git clone -- given a url it makes a local copy of a repo
git remote -v -- shows us the existing branches on our respository
git branch -- allows us to keep code separated based on what we are trying to develop
got branch -a -- shows is the existing branches on our repository
git push
git checkout
git status
git add
git config
git commit -a
